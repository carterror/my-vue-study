const hdurl = ({hdurl}) => {
    return hdurl
}

const title = ({title}) => {
    return title
}
const date = ({date}) => {
    return date
}

const media_type = ({media_type}) => {
    return media_type
}

const url = ({url}) => {
    return url
}

export default {
    hdurl, title, date, media_type, url
}